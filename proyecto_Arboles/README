# 📁 Proyecto Final – Árbol General + Trie + Consola (Día 1–12)

Sistema de archivos **simulado en C# ** que gestiona una jerarquía de carpetas y archivos usando un **árbol general**, **búsqueda eficiente con Trie**, **persistencia en JSON**, **papelera de reciclaje** y una **consola interactiva tipo shell**.

## 📅 Alcance logrado

### 🟢 Día 1: Estructura base
Se implementaron las clases fundamentales del sistema:

- `Nodo`
- `Arbol`
- Formato JSON inicial (`data/ejemplo_dia1.json`)

Se definió el MVP del proyecto:
- Manejo básico de archivos y carpetas  
- Representación mediante árbol general  
- Persistencia en JSON  
- Consola interactiva  

### 🟢 Días 2–3: Operaciones básicas del árbol
Se agregaron las operaciones principales:

- Insertar archivo y carpeta  
- Eliminar  
- Mover  
- Renombrar  
- Buscar nodos  
- Listar hijos  
- Calcular tamaño del árbol  
- Calcular altura del árbol  

Las funcionalidades fueron probadas directamente desde `Program.cs`.

### 🟢 Día 4: Persistencia en JSON
Se implementó la persistencia sin librerías externas:

**Funciones añadidas**
- `GuardarJSON(rutaArchivo)`
- `CargarJSON(rutaArchivo)`

**Características**
- El nodo padre **no se guarda** en JSON  
- Al cargar, los padres se reconstruyen recursivamente  
- Se puede exportar e importar el árbol completo  

📄 Ejemplo generado:  
`data/salida_dia4.json`

### 🟢 Días 5–6: Trie para búsqueda y autocompletado
Se creó la estructura `Trie` para búsquedas rápidas:

**Archivo:** `Trie.cs`

**Funciones**
- `Insertar(nombre, nodo)`
- `Eliminar(nombre)`
- `BusquedaExacta(nombre)`
- `Autocompletar(prefijo)`

**Integración**
- Inserción
- Renombrado
- Eliminación

El autocompletado funciona sobre **todos los nombres del sistema**.

### 🟢 Días 7–9: Consola interactiva tipo shell
Se implementó una consola similar a un sistema operativo.

**Archivo:** `Consola.cs`

#### Comandos disponibles
ls
cd <carpeta>
cd ..
md <nombreCarpeta>
mk <nombreArchivo>
del <nombre>
mv <origen> <destino>
ren <actual> <nuevo>
open <archivo>
edit <archivo> <contenido>
search <nombre>
auto <prefijo>
tree
save <archivo.json>
load <archivo.json>
trash
restore <nombre>
empty
help
exit


📦 **Papelera de reciclaje**
- `trash` → listar papelera  
- `restore <nombre>` → restaurar archivo/carpeta  
- `empty` → vaciar papelera  


### 🟢 Días 10–11: Pruebas de integración y rendimiento
Se realizaron pruebas para validar:

- Rutas inválidas  
- Árboles grandes (1,000 – 10,000 nodos)  
- Rendimiento del Trie con miles de nombres  
- Operaciones encadenadas  
- Persistencia + carga + autocompletado  

Se midieron tiempos de inserción, búsqueda y carga desde JSON.

### 🟢 Día 12: Documentación
Este README documenta completamente:

- Estructura del proyecto  
- Uso del sistema  
- Comandos disponibles  
- Formato JSON  
- Ejemplos de ejecución  
- Pruebas realizadas  

## 🧱 Estructuras de datos principales

### 🔹 Nodo
Representa una carpeta o archivo del sistema.

**Campos**
Id          // GUID único
Nombre
Tipo        // "carpeta" | "archivo"
Contenido   // solo para archivos
Children    // lista de hijos
Padre       // no se guarda en JSON

🔹 Árbol

Gestiona toda la estructura del sistema.

Funciones

CRUD de nodos

Guardar y cargar JSON

Papelera de reciclaje

Recorridos del árbol

Relaciones padre-hijo

Integración con Trie

🔹 Trie

Estructura para búsqueda eficiente por nombre.

Funciones

Insertar(nombre, nodo)

BusquedaExacta(nombre)

Autocompletar(prefijo)

Eliminar(nombre)

🔹 Consola interactiva

Mini sistema operativo hecho en C#.

Ejecutar:
dotnet run

Ejemplo:
/> md Documentos
/> cd Documentos
/Documentos> mk Tarea1.txt
/Documentos> auto Tar
/Documentos> save respaldo.json

🧾 Formato JSON

Ejemplo de un nodo:
{
  "Id": "string",
  "Nombre": "nombre",
  "Tipo": "carpeta | archivo",
  "Contenido": "solo si es archivo",
  "Children": []
}
📌 El archivo completo es un único nodo raíz (tipo carpeta) con todos sus hijos.

📌 Notas finales

No se utilizan librerías externas

El proyecto es totalmente funcional desde consola

Pensado para prácticas de estructuras de datos (árboles y tries)

📅 Día 13 – Demo y script de ejecución

▶ Cómo  se ejecuta el proyecto

Desde la raíz del proyecto  utilizas el siguiente comando: dotnet run

Esto inicia la consola interactiva del sistema de archivos.

🎥 Demo del sistema (flujo sugerido)

El siguiente script demuestra de forma ordenada todas las capacidades del proyecto:

/> md Proyectos
/> cd Proyectos
/Proyectos> md Estructuras
/Proyectos> cd Estructuras
/Proyectos/Estructuras> mk Arbol.txt
/Proyectos/Estructuras> edit Arbol.txt Estructura de datos tipo árbol
/Proyectos/Estructuras> search Arbol.txt
/Proyectos/Estructuras> auto Arb
/Proyectos/Estructuras> tree
/Proyectos/Estructuras> save demo.json
/Proyectos/Estructuras> del Arbol.txt
/Proyectos/Estructuras> trash
/Proyectos/Estructuras> restore Arbol.txt
/Proyectos/Estructuras> load demo.json
/Proyectos/Estructuras> exit

🧪 Funcionalidades demostradas en el script

Durante la demo se valida:

✔ Creación de carpetas (md)

✔ Navegación por el árbol (cd, ls)

✔ Creación y edición de archivos (mk, edit)

✔ Búsqueda exacta usando Trie (search)

✔ Autocompletado por prefijo (auto)

✔ Visualización completa del árbol (tree)

✔ Persistencia en JSON (save, load)

✔ Papelera de reciclaje (del, trash, restore)

✔ Manejo correcto del estado del sistema

🏁 Estado final del proyecto

✔ Árbol general
✔ Trie integrado
✔ Consola tipo shell
✔ Persistencia JSON
✔ Papelera de reciclaje
✔ Pruebas de rendimiento
✔ Documentación completa
✔ Demo preparada
